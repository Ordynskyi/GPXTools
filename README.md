# GPXTools
Tools for performing operations on GPX files, like merge. Use case example: you have two GPX records of your training, but one of them has good GPS coordinates, and the second has HR data. Using the tool, you can merge these files and get a file with GPS and HR data.
